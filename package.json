{
  "name": "xai-prediction-ticker",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@xai/web",
    "build": "npm run build --workspace=@xai/web",
    "lint": "npm run lint --workspace=@xai/web",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
